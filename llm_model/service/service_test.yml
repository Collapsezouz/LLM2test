# smart_auto llm_model.service.service_test test_sample
import:
- .hf_text_generation

tasks:
  __load__:
  - auto_tasks.*.*
  - .service_test

trees:
  test_sample:
    __sibling__:
      worker_num: 1
      worker_mode: thread
      join_mode: object
    __flow__:
    - service_test.test_sample
    - print.watch
    - redis__pip.conn(redis_svr)~send(pip_req+test_send_args)
    # redis__pip.conn$2(redis_svr)~@service_test.print_resp(pip_resp):
    #   prev: service_test.test_sample

configs:
  test_send_args:
    send_end_cmd: 0